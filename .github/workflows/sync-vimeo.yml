name: 🎬 Vimeo Full-Sync

on:
  schedule:
    - cron: '0 6,18 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: 🔄 Vimeo Full-Sync
        env:
          VIMEO_TOKEN: ${{ secrets.VIMEO_TOKEN }}
        run: python sync-vimeo.py

      - name: 📤 Änderungen committen
        run: |
          git config user.name "WalserFly Bot"
          git config user.email "bot@walserfly.com"
          git add videos-config.js season-covers.js
          if git diff --cached --quiet; then
            echo "✅ Keine Änderungen"
          else
            git commit -m "🎬 Videos synchronisiert ($(date +%Y-%m-%d))"
            git push
          fi
